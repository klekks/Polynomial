#pragma once
#include "Monomial.h"


template <typename TYPE>
TYPE polynomial_plus(TYPE, TYPE);

template <typename TYPE>
TYPE polynomial_minus(TYPE, TYPE);

template <typename TYPE>
TYPE polynomial_multiply(TYPE, TYPE);

template <typename TYPE>
TYPE polynomial_plus(TYPE, TYPE);

template <typename TYPE>
TYPE new_polynomial(TYPE);

template <typename TYPE>
TYPE new_polynomial(char, TYPE);
